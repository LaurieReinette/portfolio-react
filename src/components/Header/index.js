import React from 'react';
import PropTypes from 'prop-types';
import favicon from 'src/assets/favicon.ico';

import './header.scss';

const Header = () => (
  <header className="header">
    <h1>Developpeuse web junior React Symfony</h1>
  </header>
);

export default Header;
